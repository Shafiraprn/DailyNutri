package com.example.dailynutri.ui.theme

import androidx.compose.ui.graphics.Color

// --- WARNA UTAMA (HIJAU FRESH - Sesuai Logo) ---
val PrimaryGreen = Color(0xFF3DDC84) // Hijau Logo Utama
val PrimaryGreenDark = Color(0xFF00A855) // Hijau untuk Text/Tombol Gelap
val PrimaryGreenLight = Color(0xFFB9F6CA) // Hijau Muda Pastel

// --- WARNA KONTAINER (Background Kotak/Card) ---
val GreenContainer = Color(0xFFE8F5E9) // Hijau sangat pudar (untuk background kartu)
val OnGreenContainer = Color(0xFF1B5E20) // Teks hijau tua di atas background pudar

// --- WARNA AKSEN (ENERGI/KALORI) ---
val CaloriesOrange = Color(0xFFFF5722) // Oranye Api
val CaloriesOrangeContainer = Color(0xFFFFCCBC)

// --- WARNA MAKRO NUTRISI ---
val ProteinYellow = Color(0xFFFFC107)
val CarbsBrown = Color(0xFF795548)
val FatBlue = Color(0xFF03A9F4)

// --- WARNA DASAR (BACKGROUND UI) ---
val BackgroundWhite = Color(0xFFFBFDFB) // Putih dengan setitik hijau (supaya tidak silau)
val SurfaceWhite = Color(0xFFFFFFFF) // Putih murni untuk kartu
val OutlineGray = Color(0xFFBDBDBD)